{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --registry=https://registry.npmmirror.com && cd server && pnpm run build"
  },
  "deploy": {
    "startCommand": "cd server && NODE_ENV=production PORT=$PORT node dist/index.js",
    "healthcheckPath": "/api/v1/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
